version: "3.9"

services:
  fastapi-app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - APP_VERSION=2.0.0
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3